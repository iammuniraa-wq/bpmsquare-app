Drop a 1024×1024 PNG here named `app_icon.png` (no transparency) before
running `dart run flutter_launcher_icons` / `flutter_native_splash:create`
(mobile/README.md step 3). Export it from the tenant's real branding —
Settings → General shows the current logo.
