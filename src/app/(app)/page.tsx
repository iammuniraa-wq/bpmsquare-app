import Placeholder from "@/components/Placeholder";

export default function PipelinePage() {
  return (
    <Placeholder
      title="Pipeline"
      subtitle="The customer journey · Hosapete"
      blurb="The journey board (Lead → Quoted → Won → Scheduled → In service → Invoiced) is the planned home view. This slice ships the app shell and the Accounts connected hub — open Accounts in the sidebar."
    />
  );
}
